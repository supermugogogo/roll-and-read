# 游戏小图片制作与清理规范

本规范适用于所有 Read and Roll 配套小游戏中的词汇小图片。

## 成品要求

- 每张文件只保留与词义直接相关的插图主体。
- 不得残留汉字、拼音、英文标签、表格线、裁切线、页眉、题号或水印。
- 问号、方向箭头、日历上的数字或星期缩写，仅在它们本身是表达词义所必需时保留。
- 图片使用正方形画布，主体居中并留出均匀白边。
- 裁切后按比例缩放并补白，严禁横向或纵向拉伸。
- 同一组图片的视觉大小、留白和清晰度应尽量一致。

## 清理流程

1. 汇总该主题全部 `game-icons/*.png`，生成缩略图检查表。
2. 逐张检查四边、主体周围和对话框，特别留意被截断的汉字笔画。
3. 简单残留使用局部裁切或白色遮除；裁切后等比例缩放并补回原画布。
4. 文字与主体重叠时，换用完整的无字插图，不可留下半个牌子、对话框或人物残缺。
5. 重新生成嵌入式网页资源，再检查所有游戏页面是否正常加载。
6. 最后再次生成16个主题的缩略图检查表，确认没有文字或表格残片。

## 本次专项检查

- 检查范围：16个主题，共136张游戏小图片。
- 已处理：电话对话框、年级牌、文具与学科标签、国家与人物标签、日期标题、星期标签、食物与水果标签，以及多组右侧表格线。
- `快乐` 原先使用汉字本身作为图片，现已替换为纯庆祝笑脸插图。
- 清理脚本：`clean_game_icons.py`、`finalize_icon_cleanup.py`。
- 视觉检查脚本：`audit_game_icons.py`，检查图输出在 `tmp/icon-audit/`。

## 附属作业纸同步规则

- 看图连线、看图选词、看图填词等附属作业纸必须直接使用已清理的 `game-icons`，不得再次从整张词汇表截图裁图。
- 任何游戏小图更新后，都要运行 `rebuild_companion_worksheets.py`，同步重建16个主题的附属作业纸。
- 重建后运行 `audit_companion_sheets.py`，在一张总览图中检查全部作业纸的图片底边、右边和主体完整性。
- 作业纸中的文字只能出现在题目、词语库和答案选项区域；图片框内部不得带有原图标签或残余笔画。
