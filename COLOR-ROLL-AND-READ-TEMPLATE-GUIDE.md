# Roll and Read 图片制作规范

## 设计基准

- 最终确认参考图：`C:\Users\liush\Downloads\ChatGPT Image Aug 4, 2026, 10_50_28 AM.png`
- 当前颜色主题成品：`output/colors-roll-and-read.png`
- 画布比例：竖向，约 `1200 × 1312 px`
- 背景：纯白，最外侧使用细黑边框
- 风格：儿童课堂作业纸、线条清楚、色彩鲜艳、适合打印
- 视觉权威：以后必须沿用用户提供的第二版竖版参考图，不得退回第一次的方形画布、不规则多边形色块或不同配色

## 已确认的核心要求

- 保留第二版的竖版配色与排版，只替换主题内容或按要求调整网格
- 每个骰子点数对应一行，共 6 行
- 每行严格只有 6 个词汇图片，不是 8 个
- 每行从目标词中随机选择并排列；同一行不得重复
- 六行必须等高，内部五条横线必须等距
- 骰子、词汇图片和文字必须保持原始长宽比，严禁通过纵向或横向缩放整行来实现等高
- 调整行高时，只能移动内容、裁切多余留白或增加留白，再把内容垂直居中

## 顶部区域

- 左上角：`Name:` 和横线
- 右上角：`Date:` 和横线
- 中间标题：`Roll and Read!`
  - `Roll` 使用蓝色
  - `and` 使用黄色或橙黄色
  - `Read!` 使用绿色
  - 字体为圆润、粗体、儿童手写感字体
  - 所有标题字带黑色描边和轻微阴影
- 标题左右各放一个立体线稿骰子
- 中文说明居中放在标题下方：`掷骰子，读出对应的单词。`

## 主体网格

- 共 6 行，对应骰子的 1–6 点
- 第一列为骰子列，使用较宽单元格
- 每行第一格放一个白色圆角方形骰子
- 骰子行背景依次使用：
  1. 淡黄色
  2. 淡蓝色
  3. 淡绿色
  4. 淡粉色
  5. 淡紫色
  6. 淡橙色
- 词汇区每行 6 格，共 36 个练习格
- 网格使用细黑线，外框为圆角黑色边框
- 主表格上下边界之间必须精确六等分；六行等高，五条内部横线等距
- 每行的骰子、图片和文字必须在各自等高行内垂直居中
- 每个练习格包含：
  1. 上方居中的主题图片或图形
  2. 下方居中的中文词汇
- 所有图片大小、文字基线和单元格留白应保持一致
- 骰子和主题图片的视觉尺寸在六行中必须一致，不能出现越往下越高、越宽或越扁的情况

## 颜色主题的固定内容

- 词汇：红色、黄色、绿色、蓝色、黑色、白色、紫色、粉色
- 每个词使用对应颜色的颜料飞溅图形
- 白色图形必须有黑色轮廓，避免在白底上消失
- 黑色、白色也应保留清晰轮廓和足够对比度
- 每行从 8 个目标词中选择 6 个，同一行不得重复
- 六行均使用不同的随机词序，并让全部 8 个词在整张作业纸中多次出现

## 底部提示栏

- 使用圆角虚线边框
- 左侧放黄色星星
- 右侧放笑脸
- 提示文字：`小提示：掷骰子，读出图片下方的单词。可以和同学轮流玩，看谁读得又快又准！`

## 禁止的处理方式

- 不得把整行图片拉伸或压缩到目标行高
- 不得因为等高处理而改变骰子、颜料图形、汉字的宽高比
- 不得重新使用第一次制作的方形版本
- 不得把每行恢复为 8 个词汇格
- 不得只靠肉眼判断行高，必须同时进行像素坐标检查

## 质量检查

- 检查全部 36 个练习格的词语是否与图片颜色一致
- 检查骰子点数是否从 1 到 6 且没有重复或遗漏
- 检查所有汉字是否正确、没有乱码或变形
- 检查白色色块轮廓、黑色色块细节和粉色/紫色的区分度
- 检查文字、图形、边框没有裁切或重叠
- 用像素坐标检查六行高度；允许因整数像素取整产生最多 1 px 差异
- 比较六行中骰子和主题图形的边界框宽高，确认尺寸与宽高比一致
- 在原始尺寸下检查下方几行是否存在被纵向拉长的感觉
- PNG 导出后必须按原始尺寸查看
- PDF 必须重新渲染成 PNG 并检查打印页面没有裁切

## 网页替换流程

1. 最终主图保存到 `worksheets/16-colors/colors-roll-and-read.png`
2. 同步保存输出副本到 `output/colors-roll-and-read.png`
3. 重新生成 `output/colors-roll-and-read.pdf`
4. 运行 `build-embedded-assets.mjs`，更新网页离线下载资源
5. 打开 `index.html#sheet-15`，确认网页预览已显示新图
6. 检查下载按钮、打印版和配套内容仍然正常

## 复用方法

制作新主题时，保留顶部标题、骰子列、六行六列词汇网格和底部提示栏，只替换：

- 中文说明中的任务类型
- 8 个目标词汇
- 对应的 8 个主题图形
- 各行词汇排列顺序

最终至少输出：

- 高清 PNG
- US Letter 打印版 PDF
