# Read and Roll 详情页设计与复用规范

本文总结第一张 Read and Roll「日常活动」详情页的完整设计流程、功能实现、修改记录与复用规则。后续其余 14 张 Read and Roll 作业纸应以本文为统一模板，再根据每张作业纸的主题、词汇和图片进行替换。

## 一、最终页面结构

详情页采用单列、从上到下的纵向阅读流程：

1. Read and Roll 作业纸详情
2. 词语学习
3. 配套练习纸
4. 配套小游戏

各模块使用不同的浅色背景和边框进行区分，但仍保持同一套奶油色、深蓝色、珊瑚色网站视觉系统。

### 1. 作业纸详情模块

包含：

- 完整作业纸预览图
- 英文小标题 `ROLL AND READ`
- 中文主题名称
- 简短的学习内容说明
- 下载 PNG 按钮
- 打印按钮
- 文件格式和打印提示

桌面端可采用「左侧作业纸、右侧文字与按钮」的双栏布局；整个详情模块结束后，下面的学习和练习模块全部恢复为整行宽度。

### 2. 词语学习模块

包含：

- 中文词语
- 带声调拼音
- 简短英文释义
- 单词发音按钮
- “依次朗读”按钮
- “停止”按钮

点击任何词语卡片后，浏览器使用 `SpeechSynthesisUtterance` 播放普通话发音：

```js
utterance.lang = "zh-CN";
utterance.rate = 0.78;
```

词语卡片数据格式：

```js
{
  word: "起床",
  pinyin: "qǐ chuáng",
  meaning: "to get up"
}
```

第一张「日常活动」包含 12 个词语：

- 起床
- 穿衣服
- 吃饭
- 刷牙
- 洗脸
- 上学
- 上课
- 放学
- 做功课
- 两点半
- 睡觉
- 打篮球

后续页面必须逐一确认作业纸上的实际词语，不应直接复制第一张的词汇数据。

### 3. 配套练习纸模块

> 当前统一规范（覆盖下方早期“三种基础练习”的历史说明）：每个新主题默认制作并展示三张新版配套练习纸——`剪贴匹配`、`看图选词（全词汇）`、`看图描红`。三张练习纸必须覆盖该主题全部词汇，必须直接使用已经清理好的 `game-icons`，不得使用旧的 `match.png / choice.png / fill.png`，也不得从整张主作业纸重新截图。主题确有特殊教学需要时才可替换其中一种，并须在本 MD 中记录。

当前每张详情页统一包含三张配套练习纸：

1. 剪贴匹配
2. 看图选词（全词汇）
3. 看图描红

每张练习纸以小尺寸卡片显示，不应自动拉伸填满整行。桌面端建议卡片宽度约为 `145–175px`，从左到右排列。

卡片点击流程：

```text
配套练习纸卡片
  → 练习纸独立详情页
  → 完整预览
  → 下载或打印
```

不要在卡片上直接下载。用户应先进入该练习纸自己的详情页，再使用统一的下载和打印按钮。

练习纸数据格式：

```js
practiceSheets: [
  { title: "剪贴匹配", file: "cut-paste.png" },
  { title: "看图选词（全词汇）", file: "choice-all.png" },
  { title: "看图描红", file: "trace.png" }
]
```

独立练习纸路由格式：

```text
#practice-主作业纸序号-配套练习纸序号
```

例如：

```text
#practice-0-0
#practice-0-1
#practice-0-2
```

从配套练习纸详情页点击返回，应回到它所属的 Read and Roll 详情页，而不是直接返回首页。

## 二、第一张配套练习纸的重要修改

「看图连线」最初包含每一行的背景模块和预画连接线，后来进行了以下修改：

- 删除每行的背景框
- 删除图片周围的独立模块框
- 删除中间预画好的横线
- 保留图片
- 保留序号
- 保留左右连接点
- 保留右侧打乱顺序的中文词语

目的是让学生自己画线，而不是在现成线条上操作。

后续制作其他主题的连线练习纸时，应沿用该简洁格式，避免预先给出视觉答案。

## 三、真实下载与打印功能

### 1. 下载功能

网站以本地 `file://` 方式打开时，浏览器对本地文件下载存在限制。仅使用普通 `<a download>` 或 `fetch()` 可能出现：

- 点击无反应
- 只在浏览器中打开图片
- 下载按钮看似存在但没有真正保存
- 本地文件访问被浏览器阻止

最终方案是把所有作业纸 PNG 转成 Base64，集中写入：

```text
worksheet-assets.js
```

下载时：

1. 从 `WORKSHEET_ASSETS` 读取 Base64
2. 转成 `Uint8Array`
3. 创建 PNG `Blob`
4. 创建临时 Object URL
5. 通过临时 `<a download>` 触发真实下载
6. 下载后释放 Object URL

每次新增或修改作业纸 PNG 后，都要重新运行：

```text
build-embedded-assets.mjs
```

否则页面预览可能更新，但下载得到的仍是旧文件。

### 2. 打印功能

打印按钮会打开一个只包含当前作业纸图片的新窗口，并在图片加载后调用：

```js
window.print();
```

打印页面不显示网站标题、词语学习、练习纸或小游戏，只打印当前 PNG。

### 3. 文件命名

建议保持以下规则：

```text
主题名称-Read-and-Roll.png
```

例如：

```text
日常活动-Read-and-Roll.png
```

配套练习纸文件名建议使用稳定的英文短名称，避免路由和脚本转义问题：

```text
daily-match.png
daily-multiple-choice.png
daily-cut-sort.png
```

## 四、作业纸图片的完整显示

主页卡片和详情页都必须显示完整作业纸，不可以裁掉顶部标题或底部提示。

主页卡片：

- 保持原有小卡片尺寸
- 图片使用完整纵向比例
- 不为了填满卡片而过度裁切

详情页：

- 显示完整 PNG
- 图片宽度自适应容器
- 保持原始宽高比

配套练习纸卡片：

- 只作为小型预览
- 点击后进入完整详情页

## 五、配套小游戏

第一张详情页最终包含四个小游戏：

1. 骰子闯关棋
2. 听音选词
3. 看图造句
4. 汉字接接乐

所有小游戏都使用 Read and Roll 原作业纸中的词汇和插图，使作业纸、词语学习、练习纸和游戏形成同一套学习内容。

### 1. 骰子闯关棋

文件：

```text
dice-adventure.html
```

主要功能：

- 15 格闯关棋盘
- 点击骰子随机前进
- 熊猫棋子移动动画
- 朗读任务
- 动作任务
- 造句任务
- 问答任务
- 完成任务后获得星星
- 抵达终点显示闯关结果

适合训练词语综合运用，并与 Read and Roll 的“掷骰子”主题保持一致。

### 2. 听音选词

文件：

```text
listening-choice.html
```

主要功能：

- 每局随机 10 题
- 自动播放中文词语
- 可重复播放
- 每题三个图片与汉字选项
- 正误即时反馈
- 显示正确答案
- 得分统计
- 连续答对统计

预览卡片模式下必须关闭自动发音，避免详情页加载时四个游戏预览同时发声。

### 3. 看图造句

文件：

```text
sentence-builder.html
```

主要功能：

- 每局随机 8 题
- 显示 Read and Roll 原图
- 打乱句子词块
- 点击词块组成句子
- 拖拽词块进入句子框
- 在句子框中拖动调整顺序
- 将词块拖回词语区删除
- 撤回
- 重新排列
- 检查答案
- 正确后播放完整句子

拖拽功能需要同时保留点击模式，因为手机和平板上的拖拽体验与桌面端不同。

### 4. 汉字接接乐

文件：

```text
pinyin-match.html
```

该游戏最初设计成左右两列的“拼音汉字配对”，但对初学者难度过高，因此改成汉字掉落游戏。

最终玩法：

- 汉字词语从上方向下掉落
- 下方有三个篮子
- 学生点击正确篮子接住词语
- 篮子提示可在“拼音”和“图片”之间切换
- 图片使用 Read and Roll 原图
- 共 10 题
- 初始 5 颗红心
- 正确得分并播放发音
- 点击错误篮子才扣红心
- 汉字自然落地不扣红心
- 有开始/重新开始按钮
- 有暂停/继续按钮

暂停功能必须同时冻结：

- 汉字当前位置
- CSS 下落动画
- 剩余下落时间
- 本题超时计时器

恢复后从原位置继续，并使用剩余时间完成下落。

### 汉字接接乐已修复的计时 Bug

曾出现正确接住后红心仍然减少的问题。原因是旧题计时器可能在下一题开始后继续执行。

修复规则：

- 正确或错误选择后立即 `clearTimeout`
- 每题创建唯一 `roundToken`
- 超时函数执行前检查 token 是否仍属于当前题
- 重新开始游戏时使旧 token 失效
- 自然落地不执行 `lives--`
- 代码中只有“点击错误篮子”一处允许执行 `lives--`

后续制作其他掉落类游戏时必须保留这一防护逻辑。

## 六、Read and Roll 原图的复用

第一张作业纸中的 12 张插图被单独提取到：

```text
game-icons/
```

当前文件包括：

```text
get-up.png
get-dressed.png
eat.png
brush-teeth.png
wash-face.png
go-school.png
class.png
school-over.png
homework.png
two-thirty.png
sleep.png
basketball.png
```

提取脚本：

```text
export-game-icons.py
```

后续每张作业纸都应建立自己的图标目录或明确的文件名前缀，避免不同主题出现同名图片覆盖。

推荐格式：

```text
game-icons/school-pencil.png
game-icons/school-book.png
game-icons/fruit-apple.png
game-icons/fruit-banana.png
```

提取图片时注意：

- 不包含原表格边线
- 不包含图片下方汉字
- 尽量保留白色或透明背景
- 所有图片裁切尺寸保持一致
- 不拉伸人物或物品比例

## 七、小游戏预览链接卡片

配套小游戏模块最终设置为一行四张卡片，每张卡片包含：

- 游戏画面预览
- 游戏名称
- “开始游戏 →”链接提示

桌面端：

```text
4 张一行
```

中等屏幕：

```text
2 张一行
```

手机：

```text
1 张一行
```

### 预览实现方式

由于本地 `file://` 页面不便自动生成并持续维护静态截图，游戏卡片使用不可交互的缩放 `iframe` 作为实时画面预览。

特点：

- 预览自动反映游戏最新界面
- `pointer-events: none`
- 用户不会在缩略图内误操作
- 点击整个外层卡片进入正式游戏
- iframe 使用固定比例整体缩放，不能只裁切左上区域

预览 URL：

```text
游戏文件.html?preview=1
```

每个游戏检测 `preview=1` 后：

- 隐藏顶部返回栏
- 隐藏英文小标题
- 隐藏游戏大标题
- 从核心游戏区域开始显示
- 移除预览中的大阴影
- 禁止自动播放声音

正式进入游戏时不带 `preview=1`，所以完整标题和功能仍正常显示。

游戏预览卡片结构：

```html
<a class="game-card is-ready" href="game.html">
  <span class="game-preview">
    <iframe src="game.html?preview=1" tabindex="-1" aria-hidden="true"></iframe>
  </span>
  <span class="game-card-copy">
    <strong>游戏名称</strong>
    <span>开始游戏 →</span>
  </span>
</a>
```

## 八、详情页路由规则

主作业纸详情页：

```text
#sheet-0
#sheet-1
#sheet-2
...
```

配套练习纸详情页：

```text
#practice-主作业纸序号-练习纸序号
```

路由时需要设置：

- 当前下载文件 `activeSheet`
- 返回目标 `backTarget`
- 详情页图片
- 标题
- 描述
- 词语学习模块显示或隐藏
- 配套练习纸模块显示或隐藏
- 配套小游戏模块显示或隐藏

配套练习纸独立详情页应隐藏词语学习、配套练习纸列表和小游戏，只显示当前练习纸预览、下载和打印。

## 九、复制到其他 14 张作业纸的流程

### 第一步：确认基础信息

为每张作业纸确认：

- 所属主页主题模块
- 中文标题
- 原始 PNG 文件名
- 一句话详情描述
- 作业纸上的全部词语
- 每个词语的准确拼音
- 每个词语的英文释义

不确定的词语或图片必须先询问，不要凭图片猜测后直接发布。

### 第二步：添加详情页数据

在 `app.js` 对应作业纸对象中添加：

```js
{
  group: "主题模块",
  file: "原作业纸.png",
  title: "作业纸标题",
  description: "学习内容说明。",
  vocabulary: [
    { word: "词语", pinyin: "pīn yīn", meaning: "English meaning" }
  ],
  practiceSheets: [
    { title: "练习名称", file: "practice-file.png" }
  ],
  games: []
}
```

目前小游戏卡片仍写在第一张详情页的 HTML 中。扩展到全部作业纸时，建议下一步把小游戏也改成每张作业纸自己的数据数组，由 `app.js` 动态渲染，避免 15 张页面共用同一组日常活动游戏。

推荐游戏数据格式：

```js
games: [
  { title: "骰子闯关棋", file: "topic-dice.html" },
  { title: "听音选词", file: "topic-listen.html" },
  { title: "看图造句", file: "topic-sentence.html" },
  { title: "汉字接接乐", file: "topic-catch.html" }
]
```

### 第三步：制作配套练习纸

每张主作业纸默认使用同样的三种基础练习：

1. 剪贴匹配
2. 看图选词（全词汇）
3. 看图描红

只有主题确有特殊教学需要时，才用看图填词、听音编号、排序或主题分类替换其中一张；替换理由必须记录在本规范中。不得继续调用旧版 `match.png / choice.png / fill.png`。

#### 分类练习的适用原则

只有同时满足以下条件时才使用分类练习：

- 每个词语都有明确的类别归属
- 类别之间的差异容易理解
- 大多数词语不会同时属于两个类别
- 分类本身能够帮助学生理解词义
- 各类别中的词语数量不会过度失衡

不要为了保持三张练习纸形式一致而强行分类。如果需要额外解释才能说明某个词为什么属于某一类，通常说明这组词不适合制作分类练习。

例如第二张「兴趣活动」包含：电视、一家人、一起、游戏、音乐、游来游去、飞来飞去。这些词混合了人物、关系、副词、娱乐活动和移动状态，分类标准不统一，因此原来的“剪贴分类”被替换为“看图填词”。

#### 不适合分类时的替代练习

优先从以下形式中选择：

1. 看图填词：提供词语库，根据图片填写正确词语
2. 听音编号：听教师或音频朗读，给图片标上顺序
3. 看图选词：从多个词语中圈出正确答案
4. 词语排序：按照时间、动作或故事顺序排列
5. 汉字拼词：把分开的汉字组合成完整词语
6. 句子填空：把词语放进简单句子
7. 图词配对：将图片与汉字或拼音配对

替代练习应根据词汇特点选择，而不是随机更换：

- 图片清晰、词义具体：优先看图填词或图词配对
- 有明显先后顺序：优先词语排序
- 适合放入固定句型：优先句子填空
- 汉字组合具有学习价值：优先汉字拼词
- 需要强化听辨：优先听音编号

但应根据词汇类型调整：

- 动作词：顺序排列、动作与词语配对
- 水果/食物：分类、购物清单、口味选择
- 方位词：看图填方位、摆放物品
- 日期时间：日历、时钟、排序
- 学校词汇：文具分类、课程表、教室寻物

### 第四步：提取主题图片

- 从该主题 Read and Roll 原图中提取干净插图
- 使用独立且不会冲突的文件名
- 更新练习纸生成脚本
- 更新四个小游戏中的图片路径

### 第五步：制作或复制小游戏

可以复用四种游戏框架，但必须替换：

- 中文词语
- 拼音
- 图片
- 造句内容
- 任务说明
- 游戏标题
- 返回链接

不要只更换页面标题而继续使用“日常活动”的词汇。

### 第六步：更新资源与下载

新增 PNG 后：

1. 确认文件位于网站目录
2. 运行 `build-embedded-assets.mjs`
3. 确认输出的嵌入文件数量增加
4. 测试主作业纸下载
5. 测试三张配套练习纸下载
6. 测试打印

### 第七步：检查桌面端与手机端

重点检查：

- 作业纸是否完整显示
- 词语卡片是否溢出
- 拼音是否换行异常
- 三张练习纸卡片是否仍保持小尺寸
- 四张游戏卡片桌面端是否同一行
- 中等屏幕是否两列
- 手机是否单列
- iframe 预览是否显示核心游戏区
- 游戏预览是否意外播放声音

## 十、每张作业纸完成前的检查清单

### 内容

- [ ] 标题正确
- [ ] 主题模块正确
- [ ] 描述与作业纸内容一致
- [ ] 所有汉字与原作业纸一致
- [ ] 拼音声调正确
- [ ] 英文释义简洁准确
- [ ] 图片与词语一一对应

### 词语学习

- [ ] 单个词语可以发音
- [ ] 依次朗读正常
- [ ] 停止按钮正常
- [ ] 连续点击不会叠加多个声音

### 配套练习纸

- [ ] 三张卡片尺寸较小
- [ ] 练习形式适合当前词汇特点
- [ ] 分类题具有自然、明确且唯一的分类标准
- [ ] 不适合分类时已改用看图填词、排序、选词或其他形式
- [ ] 卡片进入各自详情页
- [ ] 返回到所属主作业纸
- [ ] 图片完整显示
- [ ] PNG 可以真实下载
- [ ] 打印按钮正常
- [ ] 连线题没有预画答案线

### 小游戏

- [ ] 四张游戏卡桌面端同一行
- [ ] 预览不显示游戏大标题
- [ ] 预览不自动播放声音
- [ ] 点击卡片进入正确游戏
- [ ] 游戏返回所属详情页
- [ ] 游戏词汇属于当前作业纸
- [ ] 游戏图片来自当前 Read and Roll
- [ ] 暂停和重新开始不会遗留旧计时器
- [ ] 红心只在规则规定的错误情况下减少

### 下载资源

- [ ] 新 PNG 已加入 `worksheet-assets.js`
- [ ] 下载文件名正确
- [ ] 本地 `file://` 打开时仍可下载
- [ ] 修改后的 PNG 不是旧缓存版本

### 同步

- [ ] 源文件已保存在 `Downloads/roll and read`
- [ ] 更新文件已同步到 `Documents/Codex/Read and roll`
- [ ] 右侧页面已刷新
- [ ] 源版本和右侧预览版本一致

## 十一、当前相关文件

```text
index.html                         网站结构
styles.css                        网站与详情页样式
app.js                            作业纸数据、路由、发音、下载和打印
worksheet-assets.js               Base64 作业纸下载资源
build-embedded-assets.mjs         重建嵌入下载资源
build-companion-worksheets.py     生成第一张配套练习纸
export-game-icons.py              提取第一张游戏插图
cut-paste.png                     剪贴匹配
choice-all.png                    看图选词（全词汇）
trace.png                         看图描红
dice-adventure.html               骰子闯关棋
listening-choice.html             听音选词
sentence-builder.html             看图造句
pinyin-match.html                 汉字接接乐
game-icons/                       第一张作业纸的游戏插图
```

## 十二、后续批量扩展建议

在继续制作 14 张详情页前，建议先把以下内容数据化：

1. 每张作业纸的词汇数组
2. 每张作业纸的配套练习纸数组
3. 每张作业纸的小游戏数组
4. 每张小游戏的词汇、拼音、图片和句子数据

这样后续新增主题时主要工作会变成“准备准确内容和图片”，不需要反复复制页面结构。统一的数据驱动方式也能避免：

- 游戏链接指向错误主题
- 返回按钮回到错误页面
- 旧词汇残留
- 下载文件名混乱
- 15 套 HTML 难以同时修改

第一张「日常活动」应作为功能和视觉基准。其余页面可以更换主题颜色、练习内容和游戏题目，但结构、路由、下载方式、预览方式与质量检查流程应保持一致。
